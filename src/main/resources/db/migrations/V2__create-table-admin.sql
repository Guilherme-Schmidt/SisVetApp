CREATE TABLE Administradores (

                        id int NOT NULL PRIMARY KEY,
                        nome VARCHAR(100) NOT NULL,
                        email VARCHAR(100),
                        senha varchar(100)
);
